{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/check-freshness.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If this file is a .cs file containing OpenTelemetry, ActivitySource, Meter, traces, spans, metrics, or telemetry patterns, use the otel-guide agent to validate semantic conventions are correct. Check: 1) Attribute names match semconv exactly 2) Using ActivitySource not DiagnosticSource 3) OTLP export patterns. Only report issues, don't block. Context: ${ARGUMENTS}"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "If this file is a .cs file containing OpenTelemetry, ActivitySource, Meter, traces, spans, metrics, or telemetry patterns, use the otel-guide agent to validate semantic conventions are correct. Check: 1) Attribute names match semconv exactly 2) Using ActivitySource not DiagnosticSource 3) OTLP export patterns. Only report issues, don't block. Context: ${ARGUMENTS}"
          }
        ]
      }
    ]
  }
}
