# autonomous-ci

CI helper plugin providing:

- Skill: autonomous-ci – guidance and steps to verify CI locally
- Scripts:
  - scripts/verify-local.sh – detect project type and run unit tests
  - scripts/wait-for-ci.sh – monitor GitHub Actions runs for a commit

Install/use within this monorepo via the root marketplace (.claude-plugin/marketplace.json).
